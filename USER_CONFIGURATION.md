# User configuration — v0.2 source update

Endpoint: `http://100.99.200.55:8765/v1/voice`

Controller: **Seeed Studio XIAO nRF54LM20A Sense**.
Phone: **Pixel 9 Pro running Android 17**.

These values are configured in the source. The server bearer token remains a
private value generated on the Hermes computer. The BLE passkey is generated
on the build computer before firmware compilation.

Both targets compiled in run `34752997335` from source commit `073af6bad44e0519368257abc515aa451c4a65f9`. Read `README.md` and `VERIFIED_BUILD.md` for that selected build. `START_HERE_BUILD_STATUS.md` is a historical source-only report; physical hardware acceptance and receiver deployment are still outstanding.
