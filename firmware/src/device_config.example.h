#ifndef HVB_DEVICE_CONFIG_H
#define HVB_DEVICE_CONFIG_H
/* Public example only. Provisioning creates ignored device_config.h locally. */
#error "Run python tools/provision.py from the project root to generate your PRIVATE pairing code."
#endif
